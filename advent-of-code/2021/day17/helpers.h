#pragma once
#include <string>
#include <vector>
using namespace std;

// Peak height reached by every initial velocity that hits the target area.
vector<int> peakHeightsOfAllHits(const vector<string> &puzzleInput);
