name: Everybody Codes story3

on:
  push:
    branches: [ main ]
    paths:
      - 'everybody.codes/story3/**'
      - '.github/workflows/**'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_puzzle-box.yaml
    with:
      challenge: ecd
      event: "story3"
    secrets:
      ECD_SESSION_COOKIE: ${{ secrets.ECD_SESSION_COOKIE }}
